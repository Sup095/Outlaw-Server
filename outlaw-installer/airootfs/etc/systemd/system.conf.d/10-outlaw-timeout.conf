# Outlaw OS — don't let a slow-to-exit graphical session hang shutdown/reboot
# (the "a stop job is running…" / watchdog messages). Cap the stop timeout so a
# dawdling process is force-killed quickly.
[Manager]
DefaultTimeoutStopSec=15s
